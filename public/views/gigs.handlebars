<section id="cryptos" class="container">
    <h1>All Crypto</h1>

    {{#each cryptos}}
      <div class="crypto">
      <h3>{{title}}</h3>
      <p>{{description}}</p>
      <ul>
        
        {{!-- <li>Budget: {{budget}}</li>
        <li><a href="mailto:{{contact_email}}" class="btn btn-reverse">Add Now/a></li> --}}
      </ul>
      <div class="tech">
        <small>Technologies <span>{{technologies}}</span></small>
      </div>
    </div>
    {{else}}
      <p>No gigs available</p>
    {{/each}}
    
  </section>